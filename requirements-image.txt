# Install a compatible CUDA-enabled PyTorch build separately.
# Install Nunchaku from its official matching wheel; do not use `pip install nunchaku`.
diffusers>=0.36,<1
transformers>=4.57,<6
accelerate>=1,<2
safetensors>=0.4,<1
